---
description: Preview the Firebase project
---

# Preview Project

This workflow starts the local development server for preview.

1. **Start Development Server**:
   ```bash
   # turbo
   source ~/.nvm/nvm.sh && nvm use 24 && npm run dev
   ```

2. **Open Preview**:
   After the server starts, navigate to `http://localhost:3000` in your browser.
